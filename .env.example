BACKEND_PORT=8000
FRONTEND_PORT=5173

# Carpeta con los 4 Excel de origen, montada de solo lectura dentro del contenedor backend.
DATA_DIR=./data_mock

# Límite de memoria del contenedor backend (docker-compose.yml deploy.resources.limits.memory).
BACKEND_MEM_LIMIT=512M
